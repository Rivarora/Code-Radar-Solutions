int compressString(int str[],int compressed){
     int len;
     len=strlen(str);
     if(str[i]==str[i+1]){
        printf("%c %d",str[0],len);
     }
     else{
        printf("%s",str);
     }
}